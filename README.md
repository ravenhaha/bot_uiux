# 📄 PDF Bot

Telegram бот для сбора фото и создания PDF + админка.

## Возможности

### Для пользователей:
- 📸 Отправляешь фото → бот сохраняет
- 📄 /pdf → собирает все фото в один PDF
- 🗑 /clear → удалить все свои фото

### Для админа:
- 👥 /admin → список пользователей с файлами
- 📤 /send ID → отправить PDF конкретному пользователю
- 📢 /broadcast → отправить сообщение всем

---

## Запуск

### 1. Установи зависимости

```bash
py -3.11 -m pip install -r requirements.txt
```

### 2. Узнай свой Telegram ID

Напиши боту @userinfobot — он покажет твой ID.

### 3. Запусти бота

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_BOT_TOKEN="твой_токен"
$env:ADMIN_ID="твой_telegram_id"
py -3.11 bot.py
```

**Windows (cmd):**
```cmd
set TELEGRAM_BOT_TOKEN=твой_токен
set ADMIN_ID=твой_telegram_id
py -3.11 bot.py
```

**Linux/Mac:**
```bash
export TELEGRAM_BOT_TOKEN='твой_токен'
export ADMIN_ID='твой_telegram_id'
python3 bot.py
```

---

## Команды

| Команда | Кто | Описание |
|---------|-----|----------|
| /start | все | Приветствие |
| /pdf | все | Создать PDF из своих фото |
| /files | все | Посмотреть сколько фото загружено |
| /clear | все | Удалить свои фото |
| /admin | админ | Панель админа |
| /send ID | админ | Отправить PDF пользователю |
| /broadcast | админ | Рассылка всем |
| /cancel | админ | Отменить действие |

---

## Как пользоваться

### Пользователь:
1. Заходит в бота
2. Отправляет фото (сколько нужно)
3. Нажимает /pdf
4. Получает PDF файл

### Админ:
1. Видит уведомление о новом пользователе
2. Может посмотреть кто загрузил файлы: /admin
3. Может отправить свой PDF любому: /send 123456789 → [отправляет PDF]

---

## Пример

```
Пользователь: [отправляет фото 1]
Бот: ✅ Фото сохранено! (всего: 1)

Пользователь: [отправляет фото 2]
Бот: ✅ Фото сохранено! (всего: 2)

Пользователь: /pdf
Бот: [отправляет PDF из 2 фото]
```

```
Админ: /send 123456789
Бот: 📤 Отправь PDF для пользователя 123456789

Админ: [отправляет PDF файл]
Бот: ✅ PDF отправлен пользователю 123456789
```
