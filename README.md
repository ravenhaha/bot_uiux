# 🐾 Pet Health Bot с системой супервизоров

Telegram бот для отслеживания здоровья питомцев с профессиональной расшифровкой медицинских документов.

## Возможности

### Для владельцев питомцев

- **Записи** — отправляй текст и фото, бот всё сохранит
- **Автоматические теги** — бот распознаёт вакцинации, визиты к врачу, лекарства
- **Напоминания** — создавай напоминания о лекарствах и процедурах
- **История** — просматривай все события в хронологическом порядке
- **PDF расшифровка** — отправь PDF от врача и получи текстовую расшифровку от специалиста

### Для супервизоров (медицинских специалистов)

- **Режим супервизора** — обработка запросов на расшифровку
- **Очередь запросов** — просмотр ожидающих документов
- **Расшифровка PDF** — преобразование медицинских документов в текст
- **Автоматические уведомления** — оповещения о новых запросах

## Команды

### Обычные пользователи

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу / добавить питомца |
| `/reminder` | Создать напоминание |
| `/history` | Посмотреть историю записей |
| `/reminders` | Посмотреть историю напоминаний |
| `/export` | Отправить PDF от врача на расшифровку |

### Супервизоры

| Команда | Описание |
|---------|----------|
| `/supervisor_on` | Активировать режим супервизора |
| `/supervisor_off` | Деактивировать режим супервизора |
| `/pending` | Посмотреть ожидающие запросы |

## Как это работает

### Расшифровка медицинских документов

#### Для владельцев:

1. Отправь команду `/export`
2. Прикрепи PDF документ от ветеринара
3. Документ отправляется специалисту на расшифровку
4. Получи уведомление с текстовой расшифровкой
5. Расшифровка автоматически сохраняется в историю питомца

**Пример:**
```
Пользователь: /export
Бот: Отправь PDF документ от врача
Пользователь: [прикрепляет PDF]
Бот: ✅ Документ получен! Запрос отправлен специалисту.

[Через некоторое время]
Бот: 📄 Расшифровка готова!
     Плановый осмотр. Вес: 4.2 кг (норма).
     Прививка от бешенства выполнена...
```

#### Для супервизоров:

1. Активируй режим: `/supervisor_on`
2. Получай уведомления о новых запросах
3. Нажми "Взять в работу"
4. Получи PDF документ
5. Отправь текстовую расшифровку
6. Владелец автоматически получит уведомление

## Установка

```bash
# 1. Клонирование
git clone <repo-url>
cd pet_health_bot

# 2. Виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 3. Зависимости
pip install -r requirements.txt

# 4. Токен бота
export TELEGRAM_BOT_TOKEN='ваш_токен_от_botfather'

# 5. Запуск
python bot.py
```

## Workflow расшифровки

```
Владелец → /export + PDF
    ↓
Запрос создан (pending)
    ↓
Уведомление супервизорам
    ↓
Супервизор берет запрос
    ↓
Получает PDF
    ↓
Отправляет расшифровку
    ↓
Запись сохранена + Владелец уведомлен
```

## Структура БД

- `pets` — информация о питомцах
- `records` — записи о здоровье
- `reminders` — напоминания
- `supervisors` — специалисты
- `transcription_requests` — запросы на расшифровку

## Деплой

### Docker

```bash
docker build -t pet-health-bot .
docker run -e TELEGRAM_BOT_TOKEN='токен' pet-health-bot
```

### Systemd

```ini
[Unit]
Description=Pet Health Bot
After=network.target

[Service]
Type=simple
Environment=TELEGRAM_BOT_TOKEN=токен
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## Лицензия

MIT
